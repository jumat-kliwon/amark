name: Build and Push Docker Image to GHCR

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/app:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/app:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

      - name: Print image name
        if: success()
        run: |
          echo "✅ Build successful!"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/app:latest"
          echo "Image (SHA): ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/app:${{ github.sha }}"

#  deploy:
#    runs-on: ubuntu-latest
#    needs: build-and-push
#    if: success()
#    steps:
#      - name: Deploy
#        uses: appleboy/ssh-action@v1.0.0
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USERNAME }}
#          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          debug: true
#          port: 22
#          script: |
#            cd /home/asdita/acre-lms
#            git pull origin staging
#            export GHCR_USERNAME="${{ secrets.GHCR_USERNAME }}"
#            export GHCR_PAT="${{ secrets.GHCR_PAT }}"
#            ./deployment.sh
